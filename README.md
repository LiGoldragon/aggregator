# aggregator

Runtime scaffold for the `aggregator` component. The daemon will collect and
normalize recent work evidence from configured transcript and repository
sources. Review and synthesis remain agent work outside this binary.
